{
    "name": "ppetermann/photo-desc",
    "description": "A PHP script that reads photos from an input folder and uses OpenRouter to classify them",
    "type": "project",
    "require": {
        "php": ">=7.4",
        "vlucas/phpdotenv": "^5.5",
        "guzzlehttp/guzzle": "^7.5",
        "monolog/monolog": "^2.9",
        "king23/di": "^2.0"
    },
    "autoload": {
        "psr-4": {
            "PhotoDesc\\": "src/"
        }
    }
}
